<UserControl
    x:Class="Pokemon.UserControls.Menus.BattleMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pokemon.UserControls.Menus"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Style="{StaticResource BackgroundGrid}" >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" x:Name="pokemonButton" Content="Pokémons" Style="{StaticResource PokemonButton}" 
                VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="#993300" Foreground="White" />

            <Button Grid.Row="1" x:Name="objectTypeButton" Content="Types Objets" Style="{StaticResource ObjectButton}" 
                VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="#fdc00d" Foreground="White" />

            <Button Grid.Row="2" x:Name="runawayButton" Content="Quitter le combat" Style="{StaticResource RunawayButton}" 
                VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="#0000FF" Foreground="White" />
        </Grid>
    </Grid>
</UserControl>
