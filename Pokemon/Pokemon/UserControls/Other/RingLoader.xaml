<UserControl
    x:Class="Pokemon.UserControls.Other.RingLoader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pokemon.UserControls.Other"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource BackgroundGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ProgressRing x:Name="progressRing" Grid.Row="0"  Grid.Column="0"  Foreground="Red" IsActive="True" Visibility="Visible" Width="100" Height="100" 
                      VerticalAlignment="Bottom" />
        <TextBlock x:Name="progressRingTextB" Grid.Row="1" Grid.Column="0" Text="" Visibility="Visible" FontSize="30" Foreground="Red"
                   HorizontalAlignment="Center" VerticalAlignment="Top" />
    </Grid>
</UserControl>
